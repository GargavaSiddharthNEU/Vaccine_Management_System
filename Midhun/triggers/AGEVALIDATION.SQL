create or replace TRIGGER AGEVALIDATION 
BEFORE INSERT ON RECIPIENT 
BEGIN
  IF (EXTRACT (YEAR FROM SYSDATE) - EXTRACT (YEAR FROM NEW.DATE_OF_BIRTH)) < 12 THEN SIGNAL SQLSTATE VALUE = '45000'
  SET MESSAGE_TEXT = 'INVALID AGE'
  END IF;
END;